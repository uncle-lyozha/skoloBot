export enum SubjectEnum {
  facts = 'facts',
}

export enum GameEnum {
  odisseus = 'odisseus',
  test = 'test',
}

export enum StepTypeEnum {
  story = 'story',
  battle = 'battle',
  dice = 'dice',
  end = 'leave',
}
